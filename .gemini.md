# Stock-Line-bot 專案指南

## 專案目標
建立一個台股 Line Bot，提供即時股價查詢、自定義自選股清單，並能在開盤與收盤時自動推播通知。

## 技術棧 (Tech Stack)
- **Web Framework**: FastAPI (Asynchronous)
- **Line SDK**: line-bot-sdk-python (v3)
- **Database**: PostgreSQL
- **ORM**: SQLAlchemy (Async mode)
- **Driver**: `asyncpg`
- **Migration**: Alembic
- **Stock Data**: `twstock` (or APIs like Yahoo Finance/FinMind)
- **Task Scheduler**: APScheduler (用於每日推播)
- **Config Management**: `pydantic-settings` (管理 `.env`)

## 系統架構規範 (Architecture)
所有開發必須嚴格遵循以下目錄結構與規則：

```text
C:\Users\arlon\Desktop\Stock-Line-bot\
├── app/
│   ├── api/            # 路由處理層 (取代或存放 controllers/v1)
│   ├── core/           # 核心設定 (config.py, security.py)
│   ├── database/       # 資料庫連線與 Session 管理
│   ├── models/         # SQLAlchemy 資料庫模型
│   ├── schemas/        # Pydantic 資料驗證與傳輸模型 (DTOs)
│   ├── services/       # 業務邏輯層 (股票抓取邏輯、Line 訊息處理)
│   ├── utils/          # 共用工具函式
│   └── main.py         # 應用程式進入點
├── alembic/            # 資料庫遷移腳本
├── tests/              # Pytest 測試案例
├── .env                # 環境變數 (機密資訊)
└── .env.example        # 環境變數範本
```

## 開發規則 (Coding Standards)
1. **非同步優先**: 由於使用 FastAPI，所有 I/O 密集型任務（資料庫查詢、API 請求）必須使用 `async/await`。
2. **命名規範**: 目錄與檔案一律小寫，類別使用 `PascalCase`，變數與函式使用 `snake_case`。
3. **資料分離**: 資料庫操作使用 `app/models/`，API 請求與回應的驗證使用 `app/schemas/`。
4. **邏輯解耦**: Line Bot 的 Webhook 回應與股票抓取邏輯應封裝在 `app/services/` 中，保持 Router 的簡潔。
5. **環境變數**: 嚴禁在程式碼中硬編碼金鑰，必須透過 `app/core/config.py` 讀取 `.env`。

## 功能清單 (Feature List)
- [ ] **即時股價查詢**: 使用者輸入股票代碼（如 `2330`），回傳當前股價、漲跌幅。
- [ ] **個人自選股清單**:
    - `新增 [代碼]`：加入追蹤。
    - `刪除 [代碼]`：移除追蹤。
    - `查詢清單`：列出所有自選股。
- [ ] **定時推播通知**:
    - 開盤提醒：每日 09:00 推播自選股開盤價。
    - 收盤報告：每日 13:35 推播自選股今日表現。

## 資料庫設計初步建議
- **User**: 儲存 Line User ID 與狀態。
- **StockWatchlist**: 多對多或一對多關係，儲存使用者追蹤的股票。
