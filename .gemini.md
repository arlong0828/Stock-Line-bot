# Stock-Line-bot 專案指南

## 專案目標
建立一個台股 Line Bot，提供即時股價查詢、自定義自選股清單，並能在開盤與收盤時自動推播通知。

## 技術棧 (Tech Stack)
- **Web Framework**: FastAPI (Asynchronous)
- **Line SDK**: line-bot-sdk-python (v3)
- **Database**: SQLite (Local file `stock_bot.db`)
- **ORM**: SQLAlchemy (Async mode)
- **Driver**: `aiosqlite`
- **Migration**: Alembic
- **Stock Data**: `twstock` (or APIs like Yahoo Finance/FinMind)
- **Task Scheduler**: APScheduler (用於每日推播)
- **Config Management**: `pydantic-settings` (管理 `.env`)

## 系統架構規範 (Architecture)
所有開發必須嚴格遵循以下目錄結構與規則：

```text
C:\Users\arlon\Desktop\Stock-Line-bot\
├── app/
│   ├── controllers/    # 路由處理層 (分版本管理，如 V1, V2)
│   │   ├── v1/         # 主要業務介面
│   │   └── v2/         # 未來擴充介面
│   ├── core/           # 核心設定 (config.py, security.py)
│   ├── database/       # 資料庫連線與 Session 管理
│   ├── models/         # SQLAlchemy 資料庫模型
│   ├── schemas/        # Pydantic 資料驗證與傳輸模型 (DTOs)
│   ├── services/       # 業務邏輯層 (股票抓取邏輯、Line 訊息處理)
│   ├── static/         # 靜態資源 (Flex Message 模板、圖片、CSS)
│   ├── utils/          # 共用工具函式
│   └── main.py         # 應用程式進入點
├── alembic/            # 資料庫遷移腳本
├── tests/              # Pytest 測試案例
├── .env                # 環境變數 (機密資訊)
└── .env.example        # 環境變數範本
```

## 開發規則 (Coding Standards)
1. **非同步優先**: 由於使用 FastAPI，所有 I/O 密集型任務（資料庫查詢、API 請求）必須使用 `async/await`。
2. **命名規範**: 
    - 目錄一律小寫。
    - 類別使用 `PascalCase` (大駝峰)。
    - **檔案、變數與函式一律使用 `camelCase`** (例如：`lineBotController.py`, `lineWebhook`)。
    - Controller 檔案統一命名為 `[功能名稱]Controller.py`。
3. **資料分離**: 資料庫操作使用 `app/models/`，API 請求與回應的驗證使用 `app/schemas/`。
4. **職責解耦**: 
    - **Controllers**: 負責定義與實作所有路由 (Router) 邏輯（包括 `/`, `/webhook` 等），處理 HTTP 請求、標頭、呼叫 Service 並回傳回應。
    - **Services**: 封裝所有核心業務邏輯（如爬蟲、計算、Line 訊息解析）。
    - **main.py**: 僅作為應用程式進入點，負責註冊各個 Controller 的 Router，不應包含具體的路由實作邏輯。
5. **環境變數**: 嚴禁在程式碼中硬編碼金鑰，必須透過 `app/core/config.py` 讀取 `.env`。
6. **背景任務管理**: 所有的背景任務 (Background Tasks) 必須使用獨立的資料庫 Session (`async with AsyncSessionLocal()`)，嚴禁與 Webhook Request 共用同一個 Session。
7. **Line 訊息節約**: 為了節省免費額度，機器人以「靜音模式」為原則。除非必要，進度應優先印在 Console 日誌中，不主動發送推播訊息。
8. **事務安全**: 資料庫寫入操作必須包裹在 `try...except` 中，且發生錯誤時必須執行 `await db.rollback()` 以釋放事務狀態。

## 功能清單 (Feature List)
- [ ] **即時股價查詢**: 使用者輸入股票代碼（如 `2330`），回傳當前股價、漲跌幅。
- [ ] **個人自選股清單**:
    - `新增 [代碼]`：加入追蹤。
    - `刪除 [代碼]`：移除追蹤。
    - `查詢清單`：列出所有自選股。
- [ ] **定時推播通知**:
    - 開盤提醒：每日 09:00 推播自選股開盤價。
    - 收盤報告：每日 13:35 推播自選股今日表現。

## 資料庫設計初步建議
- **User**: 儲存 Line User ID 與狀態。
- **StockWatchlist**: 多對多或一對多關係，儲存使用者追蹤的股票。
